const required=["NEXT_PUBLIC_CORE_ADDRESS","NEXT_PUBLIC_VAULT_ADDRESS"]; const bad=required.filter(k=>!process.env[k]); if(bad.length) { console.warn(`Missing deployment values: ${bad.join(", ")}`); process.exit(0); } if(process.env.NEXT_PUBLIC_GENLAYER_CHAIN_ID && process.env.NEXT_PUBLIC_GENLAYER_CHAIN_ID!=="61999") throw new Error("Aevum requires chain 61999"); console.log("frontend environment shape is valid");
