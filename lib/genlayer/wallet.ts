"use client";
import { createClient } from "genlayer-js";
import { studionet } from "genlayer-js/chains";
import { useCallback, useEffect, useState } from "react";
import { env } from "../config";
type Ethereum = { request:(args:{method:string;params?:unknown[]})=>Promise<unknown>; on?:(event:string,cb:()=>void)=>void; removeListener?:(event:string,cb:()=>void)=>void };
declare global { interface Window { ethereum?: Ethereum } }
export function useWallet(){ const [address,setAddress]=useState<string>(); const [chainOk,setChainOk]=useState(false); const [error,setError]=useState<string>(); const sync=useCallback(async()=>{ if(!window.ethereum)return; const accounts=await window.ethereum.request({method:"eth_accounts"}) as string[]; const chain=await window.ethereum.request({method:"eth_chainId"}) as string; setAddress(accounts[0]); setChainOk(Number(chain)===env.chainId); },[]); useEffect(()=>{sync(); const e=window.ethereum; if(!e?.on)return; e.on("accountsChanged",sync); e.on("chainChanged",sync); return()=>{e.removeListener?.("accountsChanged",sync);e.removeListener?.("chainChanged",sync)}},[sync]); const connect=async()=>{try{setError(undefined);if(!window.ethereum)throw new Error("No browser wallet provider found.");await window.ethereum.request({method:"eth_requestAccounts"});await sync()}catch(e){setError(e instanceof Error?e.message:"Wallet connection failed")}}; const switchNetwork=async()=>{try{await window.ethereum?.request({method:"wallet_switchEthereumChain",params:[{chainId:"0xF22F"}]});await sync()}catch(e){setError(e instanceof Error?e.message:"Network switch failed")}}; return {address,chainOk,error,connect,disconnect:()=>setAddress(undefined),switchNetwork,readClient:createClient({chain:studionet}),writeClient:address?createClient({chain:studionet,account:address as `0x${string}`,provider:window.ethereum}):undefined}; }
