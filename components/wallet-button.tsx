"use client";
import { useWallet } from "../lib/genlayer/wallet";
export function WalletButton(){ const {address, connect, disconnect, error, chainOk, switchNetwork}=useWallet(); return <div className="flex items-center gap-2">{address ? <><span className={`mono hidden text-[10px] sm:inline ${chainOk?'text-[#B8FF5A]':'text-[#B8784E]'}`}>{chainOk?'STUDIONET':'WRONG NETWORK'}</span><button onClick={disconnect} className="rounded-full border border-[#E9E1CF44] px-3 py-2 text-xs">{address.slice(0,6)}…{address.slice(-4)}</button>{!chainOk&&<button onClick={switchNetwork} className="rounded-full bg-[#B8FF5A] px-3 py-2 text-xs text-[#0B0B0A]">Switch</button>}</> : <button onClick={connect} className="rounded-full bg-[#E9E1CF] px-4 py-2 text-xs font-semibold text-[#0B0B0A]">Connect wallet</button>}{error&&<span className="hidden">{error}</span>}</div>; }
