"use client";
import { useWallet } from "../lib/genlayer/wallet";
export function WalletButton(){ const {address, connect, disconnect, error, chainOk, switchNetwork, status}=useWallet(); return <div className="flex flex-wrap items-center justify-end gap-2"><div className="flex items-center gap-2">{address ? <><span className={`mono hidden text-[10px] sm:inline ${chainOk?'text-[#B8FF5A]':'text-[#B8784E]'}`}>{chainOk?'STUDIONET 61999':'WRONG NETWORK'}</span><span className="mono text-xs">{address.slice(0,6)}…{address.slice(-4)}</span><button aria-label="Disconnect wallet" onClick={disconnect} className="rounded-full border border-[#E9E1CF44] px-3 py-2 text-xs">Disconnect</button>{!chainOk&&<button aria-label="Switch to Studionet" onClick={switchNetwork} className="rounded-full bg-[#B8FF5A] px-3 py-2 text-xs text-[#0B0B0A]">Switch to Studionet</button>}</> : <button aria-label="Connect wallet" disabled={status==="CONNECTING"} onClick={connect} className="rounded-full bg-[#E9E1CF] px-4 py-2 text-xs font-semibold text-[#0B0B0A] disabled:opacity-50">{status==="CONNECTING"?"Connecting…":"Connect wallet"}</button>}</div>{error&&<span role="alert" className="basis-full text-right text-[11px] text-[#B8784E]">{error}</span>}</div>; }
